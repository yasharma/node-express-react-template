{
  "name": "saal-author-tool",
  "version": "0.0.1",
  "scripts": {
    "prodstart": "export NODE_ENV=production && npm run start",
    "stagingstart": "export NODE_ENV=staging && npm run start",
    "teststart": "export NODE_ENV=test && npm run start",
    "dev": "./node_modules/.bin/nodemon --legacy-watch -L -e .ts -w src --exec \"./node_modules/ts-node/dist/bin.js src\"",
    "build": "./node_modules/.bin/tsc --p tsconfig.json",
    "start": "node dist",
    "prestart": "npm install",
    "test": "./node_modules/.bin/nyc ./node_modules/.bin/mocha 'test/**/*.spec.ts'",
    "lint": "tslint src/*/*.ts --format codeframe",
    "fix-lint": "tslint src/*/*.ts --fix",
    "esw": "esw src/*/*.ts --format codeframe --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@types/cors": "^2.8.4",
    "body-parser": "^1.18.2",
    "continuation-local-storage": "^3.2.1",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "log4js": "^6.1.0"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/continuation-local-storage": "^3.2.1",
    "@types/express": "^4.16.1",
    "@types/request": "^2.47.1",
    "nodemon": "^1.12.5",
    "source-map-support": "0.5.6",
    "ts-node": "^6.1.1",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  }
}
